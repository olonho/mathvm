#include "resolver.h"

namespace mathvm {

//    void Resolver::visitFunction(mathvm::BytecodeFunction *function) {
//        BytecodeFunction* tmp = currentFunction;
//        currentFunction = function;
//        currentScope = function.bo
//    }
//
//    void Resolver::visitFunctionNode(FunctionNode *node) {
//        auto *function = new AstFunction(node, currentScope);
//        auto *bytecodeFunction = new BytecodeFunction(function);
//        code->addFunction(bytecodeFunction);
//        std::cout << "Called for " << function->name() << std::endl;
//    }
}
